# Lumi (macOS Native)

Birden çok Claude Code CLI instance'ını yöneten desktop dashboard'un macOS-native (Swift) rewrite'ı. Electron sürümünün (`../ai-orchestrator`) yerini alacak.

## Durum

Keşif, tasarım ve implementasyonun 6 fazı (`docs/design/04-prototype-plan.md`) ve ardından 2026-09-04/05 sağlamlaştırma refactor'unun 7 fazı (`docs/refactor-plan-2026-09.md` — acil fix'ler, test güvenlik ağı, DI/composition root, terminal sınırları, state ayrışması, generic shell, LumiUI bileşen kütüphanesi) tamamlandı; **1936 test yeşil**. Personas + Quick Actions ve terminal tema seçimi 2026-09-04'te kaldırıldı (karar 25–26); header düzeltmeleri karar 27; file-tree tarama güvenliği (bellek sızıntısı düzeltmesi) karar 28; max terminal limiti + font smoothing ayarı kaldırıldı karar 29; 36px ince top bar + doğal traffic light konumu karar 30; ince kart header'ı + grid default'u 1 kolon/Fit karar 31; Claude+Codex kullanım göstergeleri (Orca'nın eriştiği yollardan — Claude OAuth endpoint'i, Codex app-server RPC) karar 32. Sağ proje paneli 2026-09-05'te Explorer / Agent History / koşullu Source Control sekmelerine geçti; eski Project Context ve Git panel kayıtları kaldırıldı, Unity Assets modu ve gerçek Claude/Codex oturum geçmişi eklendi (karar 39). Refactor kararları: kabuk artık slot/route/toolbar/overlay descriptor registry'leriyle kurulur (karar 33); `ui-state.json`'a additive `activeRoute`/`panelLayout`/`visibleSlots` anahtarları eklendi, eski sidebar bool'ları projeksiyon olarak yazılmaya devam eder (karar 34); `WorkspaceStore` facade'ı `NavigationStore`+`LayoutStore`+`DialogRouter`'a bölündü, route ve FileViewer sunumu sum type oldu (karar 35); composition root `ServiceRegistry` + `FeatureAssembly`/`BootstrapPhase` + tek kaynaklı `AppCommand` üzerine oturdu (karar 36); terminalde `TerminalSurfaceState` görünürlük+odağı tek kanala indirdi, `FeedWatchdog` ve enjekte edilen `OSCSemantics` zinciri eklendi (karar 37); kullanım göstergesi aralıkları {5,15,30} + 300 sn TTL cache ve Claude OAuth hata politikası netleşti (karar 38). Source Control History tek `git log HEAD --topo-order` graph'ı + `gh pr create --web` (karar 40); Orca parite düzeltmeleri — branch özeti başlığı, regex/glob içerik araması, custom popover menüler, Unity otomatik Assets görünümü, alt ajanlı Agent History kartı (karar 41); sağ proje paneli 340px + eski 280 kalıcı değerinin migrate edilmesi + sabit yükseklikli Commit butonu (karar 42); 24px alt durum barı — Settings sola, `Keep computer awake` (IOKit assertion, `computerAwakeMode` anahtarı) ve `ps` tabanlı Resource Manager sağa (karar 43); sidebar auto-reveal (gizli yuva kenar hover'ıyla orta alanı daraltmadan içeriğin üstünde açılır, `panelLayout.autoReveal`) + binary dosya önizleme crash düzeltmesi (`FilePreviewKind.binary`, `ViewerContent.unsupported`, `readFile` NUL/8 MB sniff'i) karar 44; terminal durumu Claude/Codex hook'larından (loopback `AgentHookServer` + `~/.lumi/hooks` script'leri + `~/.claude/settings.json` / `~/.codex/hooks.json`+`config.toml` güven kayıtlarına idempotent kurulum, `AgentHookStatusReducer`, 0.5 s kesme çıkarımı, `agentHooksEnabled` anahtarı) ve kart header'ında durum → kimlik ikonu → ad sırası karar 45. Plastic SCM Source Control (`.plastic/` algısı, `PlasticReading`+`PlasticWriting`/`PlasticService` actor + `PlasticOutputParser`, `PlasticStore` 7 gün penceresi + seçim + checkin, `PlasticSourceControlView` — Check in / Undo Changes / gruplu Changes listesi (`PlasticChangeGroup`, üç durumlu grup kutusu, ad+yol araması, toplu undo + `cm undo -r --unchanged`) / Git ile ortak lane graph'ı via `PlasticHistoryGraph`, `…/parent/current` branch kısaltması + kayan rozet; update/switch/merge kapsam dışı) karar 46. Composer'da ✨ ile Claude commit/checkin mesajı üretimi (`claude -p --model sonnet --effort low --tools "" --output-format json`, diff stdin'den — Git `git diff HEAD`, Plastic `cm cat` tabanı + `diff -u`; Agent SDK bilinçli olarak kullanılmaz; `CommitMessageGenerating`/`ClaudeCommitMessageService`, `CommitMessageAssistant`, ortak `CommitMessageField`) karar 47. Projects paneli + yerel Git/Plastic workspace'ler (karar 48–50) ve Orca sadeliğinde ajan satırları + sağ tık menüleri + workspace silme (karar 51) 2026-09-07. Agent History oturumlarını tek dosyalık `.lumisession.json` paketine dışa/içe aktarma (kişisel `attachment`/`cost-state`/`file-history` kayıtları ayıklanır, zincir yeniden bağlanır, alt ajanlar dahil, içe alımda cwd yeniden yazımı + kimlik çakışmasında yeni UUID; `AgentSessionTransferring`/`AgentSessionTransferService`) karar 52; Agent History sağ tık `PopoverMenu` (`RightClickCatcher`) + onaylı, çöp kutusuna taşıyan Delete Session (`AgentSessionDeleting`, `deleteAgentSession` dialogu) karar 53. Settings ▸ Agent sekmesi + DeepSeek kurulumu (`~/.claude/deepseek.env`, 0600; abonelik override edilmez) ve topbar dropdown'ının liste tabanlı hâli — New Codex / New DeepSeek / New Bash (karar 54) 2026-09-11. Topbar'dan repo tab şeridi kaldırıldı (sekme kapatma checkout satırına, repo seçici `OverlayID.repoSelector` overlay'ine taşındı), sol panelde Sessions yerine `Coming soon` gövdeli Tasks | Remote sekmeleri (`PanelItemID.tasks`, `TasksFeatureAssembly`, `ui-state` migration'ı) ve Codex kullanım isteğine 30 sn üst sınırı (`TimeoutUsageService` + probe'da SIGKILL yedeği) — karar 55, 2026-09-16. Birden çok Claude hesabı + usage popover'ından hesap değiştirme (Settings ▸ Accounts, kimlik bilgileri Keychain'de, `~/.claude` yüzeyine materialize + system-default snapshot'ı) — karar 56, 2026-09-16. Terminalde link/path'e tıklayınca tık noktasında eylem popover'ı (düz sol tık popover açar, ⌘ birincil — dosyada yine sorar, ⇧⌘ alternatif; Switch workspace / Open file / Open in Finder / Open link; SwiftTerm'in örtük link algılaması + düz tıkta fare raporunun bekletilmesi; Settings ▸ Terminal ▸ Link Actions) — karar 57, 2026-09-16. Create workspace dialogunda üç modlu dal seçimi (current / existing — proje seçilir seçilmez arka planda yüklenen, Plastic'te aktiviteye göre sıralanan ve aranabilen tam dal listesi + serbest metin / new — seçilebilir taban dal), tema uyumlu `LumiDropdown`+`LumiActionButton` kontrolleri ve Unity açıkken de yapılabilen Library kopyası (uyarı + atlanan dosya sayısı) — karar 58, 2026-09-16. Repo tab geçişi ⌃1–⌃9'a bağlandı (⌘1–⌘9 terminal odağında kalır) ve ⌘W terminal yokken repo tab'ını kapatır — karar 59, 2026-09-16. `Scripts/make-app.local.sh` ile commit'lenmeyen kişisel build ayarları (karar 60). ⌘+/⌘−/⌘0 ile tüm arayüz zoom'u — ölçek `Theme.uiScale` token çarpanı olarak iner (punto/boşluk/metrik token'ları `var`'a döndü), `UIState.uiScale` additive anahtarıyla kalıcıdır ve `AppDelegate.applyUIScale` içerik view'ını baştan kurar; ölçeklenen değerler cihaz pikseline, punto tam sayıya oturtulur ve modüldeki literal ölçüler `DesignTokenLintTests.testModuleContainsNoUnscaledMetrics` ile sıfırda kilitlenir (karar 61). İndeksli kısayolların ⌘/⌃ ekseni Settings ▸ Shortcuts'tan takas edilebilir (`AppConfig.indexShortcutStyle`; varsayılan ⌃=repo / ⌘=terminal, menü config yan etkisiyle yeniden kurulur) — karar 62. Arayüz yazı tipi Settings ▸ Appearance'tan seçilir — `System Mono` (SF Mono, varsayılan) / `JetBrains Mono` (Electron paritesi; bundle'a Medium+SemiBold yüzleri eklendi, `Theme.uiFontFamily` → `Theme.monoFont`, `UIState.uiFontFamily` additive anahtarı) — karar 63. Gezinmenin tek evreni Projects panelidir — ⌃1–⌃9 projeler arasında geçer (son kullanılan checkout'u açar, `UIState.lastCheckouts`), ⌘O birleşik `Go to Project`'tir (listede yoksa ekleyip geçer) ve `Remove from Projects` açık checkout'ları kapatır (karar 65). ⇧⌘W aktif projeyi Projects'ten kaldırır; kaldırmanın tek kapısı `ShellContext.requestRemoveProject`'tir ve minimize terminal varsa onay sorar (karar 66). Lumi çalışırken güncelleme `Scripts/install-on-quit.sh` ile yapılır — LaunchAgent Lumi'nin kapanmasını bekler, kurar, yeniden açar, kendini siler; `make-app.local.sh` kişisel kalmaya devam eder (karar 64). Kenar hover'ı (auto-reveal) SwiftUI `.onHover` yerine AppKit sensörüne (`PointerPresence`: tracking area + 0,1 sn doğrulama tik'i + ana pencereye bağlı popover/dropdown pencerelerini içeri sayma) taşındı — terminal `mouseMode == .anyEvent` iken `.mouseMoved` yutulduğu için auto-reveal hiç çalışmıyordu; popover açılınca kapanma ve çıkışta kapanmama da bitti — karar 67, 2026-09-17. `SystemProcessRunner` timeout'u artık kesin üst sınır (süreç çıkmış ama torun pipe'ı açık tutuyorsa da sonuç verilir — Accounts ▸ Re-authenticate uygulamayı süresiz kilitliyordu), yeniden doğrulamada Cancel görünür ve `usageAutoRefresh.enabled` varsayılanı açık — karar 68, 2026-09-17. Kenar hover'ı artık tamamen imleç yoklamasına dayanır — AppKit crossing event'leri de `.mouseMoved` yutulunca doğmuyor; ayrıca auto-reveal yalnız yuva GİZLİYKEN anlamlıdır ve Settings ipucu bunu söyler — karar 69, 2026-09-17. Terminal link eylemlerinde kök guard'ı yalnız çöpe atmada kalır (`/private/tmp/...` gibi kök dışı yollarda Open with default app / Open in Finder sessizce reddediliyordu) ve `/private` ön eki popover başlığında korunur — karar 70, 2026-09-17. Auto-reveal'in gerçek kök nedeni bulundu: `panelReveal` overlay'i `PanelItemRegistry`'nin (değer tipi) feature kayıtlarından ÖNCEKİ boş kopyasını tutuyordu — overlay artık `ShellRegistries`'i saklayıp `panels`'ı render anında okur — karar 71, 2026-09-17. Sağ panelin seçili sekmesi artık `LayoutStore.projectToolsTab`'da ve `ui-state`'in additive `projectToolsTab` anahtarında tutulur (panel kapanınca Explorer'a dönüyordu) — karar 72, 2026-09-17. Kenar hover'ı overlay'i açık repo istemez (Tasks/Remote route'unda ve proje seçili değilken de çalışır) — karar 73, 2026-09-17. Kullanım barında tempo çizgisi — pencerenin geçen kısmı dolgunun üstüne dikey çizgi olarak iner, dolgu çizgiyi aşmışsa limit saatten hızlı tüketiliyordur; `UsageWindow.duration` Codex'te `windowDurationMins`'ten, Claude'da yanıtın kendi sözlüğünden (`five_hour`/`seven_day` → `ClaudeUsageWindows`) gelir; türü tanınmayan limitte çizgi çizilmez — karar 74, 2026-09-18. Topbar'da DeepSeek bakiye göstergesi (`GET /user/balance` — DeepSeek'in API'sinde harcama/limit ucu yok; `DeepSeekBalanceService`+`DeepSeekBalanceStore`, additive `usageIndicators.deepseek` anahtarı varsayılan kapalı, ikinci kapı env dosyasındaki anahtardır, otomatik tazeleme `AutoRefreshing` yüzünden geçer) — karar 75, 2026-09-18. Kullanım göstergesinin kaynağı sabit 5 saatlik pencere değil `UsageSnapshot.indicatorLimit`'tir (oturum penceresi varsa o, yoksa raporlanan ilk limit) — Codex'in yeni plan tiplerinde oturum penceresi hiç dönmediği için topbar `—` gösteriyordu; tooltip/VoiceOver artık hangi pencere olduğunu söyler — karar 76, 2026-09-18. Dikkat isteyen terminalin başlığı sarıya döner (Orca paritesi): seçili değilken turn'ü kapanan (`waitingUnseen`) ya da karar bekleyen terminalin hem kart başlığı hem Projects panelindeki ajan satırı vurgulanır; vurgu başlık rengi + kenarlıktır (kartta warning ring, sidebar'da 2pt kenar çubuğu; zemin boyanmaz — o kanal seçim/hover'ın), kural view'sız `TerminalAttention`'dadır ve `waitingSeen` tekrar vurgulanmaz — karar 77, 2026-09-18. macOS-başlatılan Claude terminalleri telefonda chat view'de görünür (transcript-tail; `sendSessions` claude→kind:chat, `handleSubscribe` chat modu terminal claude için `awaitTranscript`, `handleChatSend` stream-json değilse PTY'ye yazar) — karar 88, 2026-09-20. Telefon-başlatılan chat de Mac'te başsız stream-json yerine bir claude TERMİNALİ olarak açılır (`RemoteCommandHandler.startSession kind==chat` → `terminal.spawn("claude"/"claude <prompt>")`, workspace/worktree dalı korunur, dönen sessionId = terminal id) — böylece masaüstü grid'inde kart olarak görünüp Mac'ten takip edilir ve telefon karar 88 makinesiyle izler; bedeli telefon-başlatılan chat'in de transcript-tail olması (stream-json altyapısı dormant kalır) — karar 89, 2026-09-20. Resume listesi (`ui-state.json` → `resumeSessions`) artık yalnız graceful quit'te değil `.spawned`/`.exited`/yeni `TerminalEvent.codexSessionIDChanged` olaylarında da yazılır (crash/SIGKILL sonrası açık Claude/Codex chat'leri geri gelir; açılışta liste silinmez, canlı terminallerden yeniden üretilir; shutdown önce tüketiciyi susturur) — karar 90, 2026-09-22. Telefonun oturum listesi Mac'in Projects panelini yansıtan `Proje → Checkout → Ajan` ağacına döndü — Mac tek doğruluk kaynağı; `sessions` yanında additive bir `projects` anlık görüntüsü yayınlanır (`RemoteProtocol.projectsPayload`; tetikleyiciler: `welcome`, `.spawned`/`.exited`/`.statusChanged` ve yalnız `sidebarProjectPaths`/`workspaces` değiştiğinde `configChanged`), `SessionMeta`'ya additive `provider`+`lastActivityAt` eklendi, telefonun tek yazma yetkisi `add_project`'tir (favori ekleme) ve relay `projects`'i oda başına cache'leyip `welcome`'a iliştirir; `ios-v*` tag'i TestFlight build'i alır — karar 91, 2026-09-22. Proje hızlı komutları — proje sağ tık `Actions…` modalında ad + sh script (Claude `claude -p` sonnet ile, proje kökünde Bash/Read/Glob/Grep araçlarıyla yazar), `{path}`/`{project_path}`/`{name}`/`{branch}` anahtarlarıyla checkout'tan bağımsız; checkout sağ tık `Run` bölümünden yeni terminalde `sh ~/.lumi/quick-commands/<id>-<checkout>.sh` olarak çalışır; `config.json`'a additive `projectQuickCommands` — karar 92, 2026-09-22. Projenin tek `Start App` komutu (additive `role`) doluysa checkout menüsünün en başında durur ve terminal açmadan `nohup` ile arka planda koşar (çıktı script'in yanındaki `.log`'a); diğer komutlar `Actions` alt menüsünde (`PopoverMenu.Item.submenu`, hover'da sağda açılır) — karar 93, 2026-09-22. Kod `LumiPackages/` altında (modüller: LumiKit/LumiTerminal/LumiServices/LumiState/LumiUI + LumiAppCore library + ince LumiApp executable + LumiTestSupport paylaşılan fake target'ı). Geliştirme: `cd LumiPackages && swift run Lumi`; paketleme: `Scripts/make-app.sh` (imza: `IDENTITY` → keychain'deki Apple Development sertifikası → ad-hoc; `--install` ile `/Applications`'a kurar; `IDENTITY=... Scripts/make-app.sh --notarize` ile Developer ID imza + notarize + staple + dağıtım paketleri: `dist/Lumi-<VERSION>-<arch>-mac.zip`, `.dmg` ve `SHA256SUMS`. Notarize için keychain'e bir kez `xcrun notarytool store-credentials lumi-notary ...` ile profil kaydedilmeli; profil adı `NOTARY_PROFILE`, sürüm `VERSION` env ile değiştirilebilir. Ad-hoc imzada TCC izinleri her build'de sıfırlanır, o yüzden sertifika tercih edilir). CI/CD: `v*` tag push'u GitHub Actions'ta (`.github/workflows/release.yml`) arm64 + x86_64 notarized build alıp draft release oluşturur; tag, `make-app.sh` içindeki `VERSION` default'uyla eşleşmek zorunda. Gerekli repo secret'ları: `MAC_CERTIFICATE_BASE64`, `MAC_CERTIFICATE_PASSWORD`, `APPLE_ID`, `APPLE_TEAM_ID`, `APPLE_APP_SPECIFIC_PASSWORD`. Kalan manuel doğrulamalar: 10 dk tam P1 ölçümü (Instruments — `FeedWatchdog` timer'ları dahil), gerçek cihazda voice-mode mikrofon TCC zinciri, Faz 6/7 sonrası görsel parite sapmaları (punto/radius token'ları, opak toast + focus bar, %60 modal karartması), Settings/FileViewer açıkken terminal üstünde scroll'un terminale geçmemesi (`TerminalEventMonitor` hit-test'i), quit ve close-tab onay dialoglarının gerçekten görünmesi (`DialogRouter` + `OverlayHost`), prompt-queue panelinin ilk açılışta odağı alması. (Notarized build Gatekeeper testi 2026-08-05'te v0.6.0 release asset'iyle doğrulandı: `spctl` accepted, "Notarized Developer ID".)

Teknoloji kararları (2026-06-11, bağlayıcı — detay ve gerekçeler `docs/design/00-architecture.md`):
- **SwiftTerm** (SPM) + oturum başına kalıcı view-attached emülatör; PTY katmanı kendi `PTYProcess` wrapper'ımız (LocalProcess değil)
- **AppKit kabuk + SwiftUI içerik**; terminal NSView'ları SwiftUI dışında `TerminalViewRegistry`'de
- **Manuel DI + `AppContainer` composition root**; lokal SPM paketleri `LumiKit ← LumiTerminal / LumiServices / LumiState ← LumiUI`
- **macOS 14+**, Swift 6 strict concurrency; servis→store `AsyncStream`, store→UI `@Observable`, Combine yok

Son ek: Codex hesapları Orca gibi ayrı `CODEX_HOME` dizinlerinde tutulur; Settings/usage popover seçimi yeni terminallere ve Codex usage probe'una uygulanır, çalışan terminaller hesabını korur (karar 78). Graceful quit'te hook'tan alınan Codex thread kimliği kaynak `CODEX_HOME` ile kaydedilir ve açılışta `codex resume` ile geri açılır (karar 80, 2026-09-18). Terminal kartı header'ı ile Projects ajan satırı aynı `AgentActivityIcon` durum dilini kullanır; dikkat isteyen başlık iki yüzeyde de `TerminalAttention` kuralıyla sarıdır (karar 81, 2026-09-18). Projects'ten ajan seçimi checkout zaten maximize modundaysa seçilen terminali aynı solo yüzeyde öne getirir; grid modunu kendiliğinden maximize etmez (karar 82, 2026-09-18). Hayalet-terminal bug'ı için kalıcı teşhis izi — `LumiLog` (unified log `com.lumi.app`: tüketici döngüsü başlangıç/çıkış/stop, terminal olayları `←`/`✓`, spawn/kill/exit, bootstrap ve quit adımları) ve TTY'siz başlatmada `~/.lumi/logs/stderr.log`'a stderr yönlendirmesi; kendini onaran eşitleme bilerek yok (karar 83, 2026-09-19). Kullanım göstergesinin rengi mutlak yüzdeyi değil tempo çizgisine olan mesafeyi gösterir — çizginin ±5 puanlık çevresi yeşil, geride kalındıkça buz mavisi (`Theme.ice`), öne geçildikçe sarıdan kırmızıya giden sürekli bir rampa (`UsagePace` saf aritmetik + `Theme.blend` ara tonu + `UsageTint` tek kapısı); topbar da bu rampadan beslenir, süre bilinmeyen pencerede mutlak `UsageLevel` bandı yedektir, `percentUsed >= 100` rampayı ezer (karar 85, 2026-09-19). Git sorguları iki kapıdan geçer: git olmayan projede hiç git komutu koşmaz (`capabilities.isGitRepo`), `origin` adresi ise yapılandırma sayılıp repo başına bir kez sorulur (`rescanRemote`; manuel yenileme ve `evict` tazeler) ve "origin yok" artık arıza olarak loglanmaz (karar 84, 2026-09-19). Hayalet-terminal bug'ının kök nedeni bulundu: her Codex kullanım probe'u `waitUntilExit()`'te bir cooperative pool thread'i sızdırıyordu ve 8 çekirdekli makinede 8 sızıntıdan sonra Swift concurrency'nin tamamı (dolayısıyla `AsyncStream` tüketicileri) duruyordu — sonlandırma artık süresiz beklemez (`reapGrace`) ve pool dışında koşar (`shutdownDetached`) (karar 86, 2026-09-19).

## Kurallar

- **Davranışın kaynağı implementasyonun kendisidir:** Rewrite tamamlandığı için Electron davranış envanteri spec'leri (`docs/spec/`) 2026-09-04'te kaldırıldı (git history'de duruyor; Electron kodu da `../ai-orchestrator`'da yerinde). Yeni iş, `LumiPackages/` altındaki kod + testlerden ve aşağıdaki iki bağlayıcı kayıttan yazılır.
- **`docs/design/` bağlayıcı tasarım kaydıdır:** Mimarinin ve implementasyon şeklinin kaynağı. Tasarımla çelişen implementasyon yapma; tasarım değişikliği gerekiyorsa önce kullanıcıya sor ve ilgili design dosyasını güncelle.
- **`docs/decisions.md` bağlayıcı karar kaydıdır:** Kapsam, atılan özellikler ve bilinçli davranış değişiklikleri burada. Bu kararlarla çelişen bir implementasyon yapma; karar değişikliği gerekiyorsa önce kullanıcıya sor ve dosyayı güncelle.
- **Zorunlu mimari gereksinimler** (`docs/design/00-architecture.md` Ek A): PTY→UI ack-tabanlı backpressure, render-crash izolasyonu, replay güvenliği (sequence-güvenli kesim, terminal otomatik yanıt filtresi). Bunlar sonradan eklenemez — çekirdek tasarımın parçası.
- **SOLID + dependency injection:** Rewrite'ın ana hedeflerinden biri; view katmanı iş mantığından ayrı tutulur.
- **Persistence uyumluluğu:** `~/.lumi` altındaki mevcut JSON/YAML formatları aynen okunur/yazılır (karar 9) — format değişikliği yapma. Config tipleri `Codable` **değildir**; okuma/yazma yalnız `LumiServices/Config/` altındaki `ConfigCodec` bölüm codec'lerinden geçer.
- **Generic shell (karar 33):** Yeni bir görünüm, panel öğesi veya toolbar öğesi `RootView`/`PanelHostView`/`HeaderBarView`/`ContentRouterView`'a **dokunmadan** eklenir: bir `FeatureAssembly` (+ katkı veriyorsa `ShellContributing`) yazılır, descriptor'lar `ShellRegistries`'e kaydedilir ve `AppComposition`'daki assembly listesine tek satır eklenir. `AnyView` yalnız descriptor'ın `makeView`'ında kullanılır.
- **LumiUI'da literal yok:** Font boyutu ve köşe yarıçapı literal olarak yazılmaz; `Theme.Typography` / `Theme.Radius` / `Theme.Spacing` / `Theme.Motion` token'ları zorunludur (`DesignTokenLintTests` bütçeyi sıfırda kilitler). Her `#Preview` `#if DEBUG` içinde olmalıdır (`PreviewGuardLintTests`).
- **Bloklayıcı bekleme cooperative pool'da koşmaz (karar 86):** `waitUntilExit()`, koşulsuz `usleep`/`sleep` döngüsü ve semafor beklemesi `async` bir fonksiyonun içinden çağrılamaz — pool'un thread sayısı çekirdek sayısı kadardır ve tükendiğinde uygulamanın TÜM async dünyası durur. Bekleme sınırlı olur ve ayrı bir `DispatchQueue`'ya taşınır.
- **Yeni servis eklerken:** process I/O `ProcessRunning` (+ `BinaryLocating`) enjeksiyonundan geçer, fake'i `LumiTestSupport`'a eklenir ve `LiveServiceRegistry`/`FakeServiceRegistry`'ye bir satırla bağlanır.
- **Geliştirme komutları** (CI üçünü de koşar): `cd LumiPackages && swift build`, `swift test`, `swift build -c release --product Lumi`.
